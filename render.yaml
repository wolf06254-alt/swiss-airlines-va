services:
  - type: web
    name: swiss-airlines-va
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_USERNAME
        value: Gregory
      - key: ADMIN_PASSWORD
        value: 123789
      - key: SESSION_SECRET
        generateValue: true
